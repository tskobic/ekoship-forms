import { DaysSinceCalculatorPipePipe } from './days-since-calculator-pipe.pipe';

describe('DaysSinceCalculatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysSinceCalculatorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
