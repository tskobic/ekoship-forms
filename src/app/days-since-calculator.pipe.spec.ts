import { DaysSinceCalculatorPipe } from './days-since-calculator.pipe';

describe('DaysSinceCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysSinceCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
